# Compiled binary
goagent-linux
goagent.exe

# Models (too large for git)
*.gguf

# SSH keys (NEVER push these)
*.key
*.pem

# Environment variables (NEVER push these)
.env
.env.*
!.env.example

# OS files
.DS_Store
Thumbs.db
